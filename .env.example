# ==============================================
# 노션 견적서 뷰어 - 환경 변수 설정
# ==============================================
# 이 파일을 복사하여 .env.local 파일을 생성하세요:
# cp .env.example .env.local

# ----------------------------------------------
# Supabase 설정 (필수)
# ----------------------------------------------
# Supabase 프로젝트 URL
# https://app.supabase.com/project/YOUR_PROJECT/settings/api 에서 확인
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase 익명 키 (anon public)
# 클라이언트 사이드에서 사용되는 공개 키
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase 서비스 롤 키 (service_role secret)
# 서버 사이드에서만 사용되는 비밀 키 - 절대 클라이언트에 노출 금지
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ----------------------------------------------
# 암호화 설정 (필수)
# ----------------------------------------------
# API 키 암호화용 키 (32바이트 base64 문자열)
# 터미널에서 생성: openssl rand -base64 32
ENCRYPTION_KEY=your-encryption-key

# ----------------------------------------------
# 노션 API 설정 (선택 - 환경변수 방식)
# ----------------------------------------------
# 환경변수 방식: 단일 사용자 또는 고정된 노션 연동 시 사용
# DB 설정 방식: 각 사용자가 개별 노션 API 키를 설정 페이지에서 저장
#
# 1. https://www.notion.so/my-integrations 에서 새 Integration 생성
# 2. 생성된 Integration Token을 NOTION_API_KEY에 입력
# 3. 노션에서 견적서 데이터베이스 페이지 열기
# 4. 데이터베이스 우측 상단 ... 메뉴 → 연결 → 생성한 Integration 추가
# 5. 데이터베이스 URL에서 32자리 ID를 NOTION_DATABASE_ID에 입력
#    예: https://notion.so/workspace/abc123def456... → abc123def456...

NOTION_API_KEY=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ----------------------------------------------
# 이메일 발송 설정 (선택 - v2 기능)
# ----------------------------------------------
# Resend 이메일 서비스 API 키
# https://resend.com/api-keys 에서 API 키 생성
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 발신자 이메일 주소
# Resend 무료 티어: onboarding@resend.dev 또는 인증된 도메인 이메일 사용
# 프로덕션: 도메인 인증 후 실제 이메일 주소 사용 권장
EMAIL_FROM=onboarding@resend.dev

# ----------------------------------------------
# 개발 환경 설정 (선택)
# ----------------------------------------------
# 번들 분석 활성화 (npm run analyze 시 자동 설정됨)
# ANALYZE=true
